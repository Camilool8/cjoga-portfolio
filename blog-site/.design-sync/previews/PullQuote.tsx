// Authored preview — hero pull-quote with eyebrow + attribution.
import { PullQuote } from "blog-site";

export const Default = () => (
  <PullQuote eyebrow="On certifications" attribution="— Why certifications expire">
    The cert is a snapshot. The cluster is the proof.
  </PullQuote>
);
