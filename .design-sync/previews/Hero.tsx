// Authored preview — Hero renders as the full portfolio section, pulling its copy
// from the bundled i18n locale and its data from the repo. Committed sync input.
import { Hero } from "cjoga-portfolio";

export const Default = () => <Hero />;
