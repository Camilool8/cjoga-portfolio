// Authored preview — About renders as the full portfolio section, pulling its copy
// from the bundled i18n locale and its data from the repo. Committed sync input.
import { About } from "cjoga-portfolio";

export const Default = () => <About />;
